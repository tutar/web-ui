# Managed Agent Web UI

独立 Web 前端，消费 `managed-agent-api` 提供的 HTTP/SSE 接口。

## 本地运行

1. 配置环境变量：
   - `VITE_MANAGED_AGENT_API_BASE_URL`
   - `VITE_MANAGED_AGENT_USER_ID`
2. 启动：
   - `npm run start`

默认本地 API 地址见 [.env.example](./.env.example)。
